import styled from "styled-components";

export const Wrapper = styled.div`
  .five-wide {
    padding: 0 20px;
    & .ui.header {
      font-weight: 350;
      margin: 15px 0;
    }
    & .ui.button {
      background-color: black;
      color: white;
    }
  }
`;
